# FinOS Hotel - Backend dependencies
# OCR thực hiện ở Ollama (VLM, ngoài tiến trình) nên backend rất nhẹ:
# KHÔNG cần torch/paddle/onnx/opencv. Chỉ gọi HTTP + xử lý ảnh bằng Pillow.

fastapi==0.115.6
httpx==0.28.1                    # test client cho smoke/integration tests
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # nhận upload ảnh
pydantic==2.10.4
pydantic-settings==2.7.1

# Auth
PyJWT==2.10.1                   # JWT bearer token (nhẹ hơn python-jose)
argon2-cffi==23.1.0             # hash mật khẩu

# Xử lý ảnh trước khi gửi VLM (xoay đúng chiều + thu nhỏ)
Pillow==11.0.0

# Web Push notification delivery (optional at runtime; used when FINOS_VAPID_* is configured)
pywebpush==2.0.3
